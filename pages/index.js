import {Container} from "@mui/material";
import Layout from "../src/components/layout";

const Home = () => {
  return (
    <Layout>
      <Container maxWidth={`lg`}>
        <h2>ho</h2>
      </Container>
    </Layout>
  );
};

export default Home;
