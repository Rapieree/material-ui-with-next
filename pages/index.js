import {Container} from "@mui/material";
import Layout from "../src/components/layout";

const Home = () => {
  return (
    <Layout>
      <Container maxWidth={`lg`}>
      </Container>
    </Layout>
  );
};

export default Home;
